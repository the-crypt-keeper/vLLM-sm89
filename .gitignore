# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
*.swp
*~

# Internal session logs / research harnesses (not for publication)
internal/

# local artifacts (wheels kept out of git anywhere in the tree)
*.whl

# bench: smoke-test results are transient (real releases are committed)
bench/results/smoke/
# baseline clone at the repo root (read-only, pinned at v0.25.1).
# Anchored so it does NOT match overlay/vllm/ which IS tracked.
/vllm/

# server logs from local runs (large, and they carry request text)
/logs/
.omc
.codebase-memory
